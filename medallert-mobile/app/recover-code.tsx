import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function RecoverCode() {
  return (
    <SafeAreaView>
      <Text>Recover Code</Text>
    </SafeAreaView>
  );
}
